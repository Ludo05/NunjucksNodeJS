{%  macro textInput(labelName, inputName) %}
    <label>
         {{ labelName }}
        <input type="text" name="{{ inputName }}" style="width: 200px; height: 20px;  border: blue 2px solid; background-color: lightgrey;" placeholder="Type details">
    </label>
{% endmacro  %}
